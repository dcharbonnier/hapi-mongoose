/* globals describe, it */

describe('Find All', function() {
    describe('pagination parameters', function() {
        it('should limit according to `limit` parameter', function(){});
        it('should start result at `offset` parameter', function(){});
    });
    describe('`query` parameter', function() {

    });
    describe('`sort` parameter', function() {

    });
    describe('response', function() {
        it('should have a `meta` property with a `total` property', function(){});
        it('should have a property \'modelName\' with a list of objects', function(){});
    });
});